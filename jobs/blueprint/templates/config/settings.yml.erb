---
paths:
  files: /var/vcap/store/blueprint/files/
  credentials: /var/vcap/store/blueprint/credentials/
  logs: /var/vcap/sys/log/blueprint/
port: <%= p('port') %>
admin:
  username: <%= p('admin.username') %>
  password: <%= p('admin.password') %>
agent:
  port: <%= link("blueprint_agent").p('port') %>
  username: <%= link("blueprint_agent").p('username') %>
  password: <%= link("blueprint_agent").p('password') %>
